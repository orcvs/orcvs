# 09 — Move the Cursor Effect's motion into settings

**What to build:** Glitch amount and Glitch frequency become console settings, independent of the Theme, beside the operating system's reduced-motion preference. After `06` moves the Cursor Effect's colours into the Theme, they are the only part of `CursorEffectSettings` left.

**Blocked by:** 06 — Paint the Source from a named Theme.

**Status:** resolved

**Tags:** release/v1

- [x] Glitch amount and Glitch frequency live in the console's settings, with today's defaults (60 and 55), and no Theme carries either.
- [x] Changing the dark or light Theme, or the mode, never changes either value.
- [x] Their controls move out of the `Theme` menu. They describe motion, not appearance.
- [x] With `persistence`, they are restored across restart. An absent or malformed stored value falls back to the defaults. The `cursor_effects` key is either reduced to these two values or left unread in favour of the settings key. The choice is recorded here.
- [x] Amount zero paints one clear, stationary Cursor frame without decorative noise and schedules no cursor-effect repaints, even when the stored frequency is positive. The scheduling change is intentional: today's `repaint_after` checks frequency alone.
- [x] Frequency zero freezes the current effect, which may retain decorative fragmentation when amount is nonzero, and schedules no cursor-effect repaints. It does not implicitly set amount to zero or clear the effect.
- [x] Reduced motion paints a clear, stationary frame and schedules no cursor-effect repaints. It affects the effective motion settings, without overwriting the viewer's stored amount or frequency.
- [x] Stopping cursor-effect repaints does not suppress independent Playback, Run Clock or input-driven repaints.
- [x] Focused tests cover amount zero with positive frequency, frequency zero with nonzero amount, reduced motion with retained stored preferences, and continued Playback/Run Clock repaint scheduling when the cursor effect has no deadline.
- [x] `cargo nextest run --package console --locked` and the `--no-default-features` arm both pass.

## Comments

**2026-09-21 — opened from `01`.** ADR 0053 decides that a Theme sets how things look, never how much they move. A viewer who needs the glitch off should not have to make a custom Theme for every Theme they use. VS Code's `editor.cursorBlinking` and Zed's `cursor_blink` are settings for the same reason.

**2026-09-21 — zero settings clarified by the user.** Amount zero gives a clear, stationary frame; frequency zero freezes the current effect and may retain fragmentation; reduced motion gives a clear, stationary frame. The user also confirmed stopping unnecessary cursor-effect repaints at amount zero while preserving Playback's independent repaints. This replaces the earlier acceptance that conflated the two zero values and described all scheduling as unchanged.

**2026-09-22 — explicit role backgrounds confirmed.** Role backgrounds are explicit colour properties, such as `source.function.background`, `source.number.background`, `diagnostic.background` and `output_portal.background`. They composite over the uniform `cell.background` under the confirmed fixed precedence. Changing a role foreground does not recalculate its background. There is no `fill_tint` property or shared tint-strength setting in the Orcvs Theme format. Extract and verify the built-in role background colours from current rendering so its appearance is preserved; illustrative colours in the interview are not accepted defaults.

**2026-09-22 — Settings-menu slice implemented and reviewed, after verifying `06`.** `06` is only partly implemented on this branch (its own Status line correctly still reads `ready-for-agent`): there is no `Scheme`/named-property-template model beyond the one `okabe_ito()` built-in, and no dark/light pickers. What `09` depended on is landed: the Cursor Effect's colours are on the resolved `Theme` (`console/src/theme.rs` carries `cursor_border`, `cursor_area`, `region_background`, `cursor_background`, `region_cursor_background` and the two border widths, and `console.rs`'s `show_source`/`show_source_scene` read them from `self.theme`), and `CursorEffectSettings` (`console/src/cursor_effects.rs`) was already reduced to `amount`/`frequency`, with `encode`/`decode` packing only those two fields. What this pass did, against the first four acceptance lines above:

- Moved the "Glitch amount"/"Glitch frequency" `Slider`s out of the `Theme` menu into a `Settings` menu in the top bar (`console.rs::ui`), and removed the `Theme` menu entirely rather than leave an empty one a viewer could open — `03`/`04`/`06` add it back once there is a picker to put in it.
- Reordered `Console`'s fields so `cursor_effects`/`cursor_effect_animation` sit directly beside `reduced_motion` in the struct.
- **Persistence key decision: reduce `cursor_effects`, don't add a settings key.** `CURSOR_EFFECTS_KEY = "cursor_effects"` already only round-trips `amount;frequency`, and a value written before `06` (which carried colour groups with commas) is refused whole and falls back to the defaults — this crate's ordinary "malformed value falls back to the default" rule, not a migration. A separate settings key would have split one small value across two persisted homes for no reason. Documented at the key's own definition in `console/src/persistence.rs`.
- Corrected `console/src/theme.md`'s self-contradictions about menu contents. Left the rest of that section's larger `06`-era staleness alone, since `03`/`08` also edit `theme.md`.
- Acceptance line 2 (Theme/mode never changes Cursor effects) holds structurally: `Theme` declares no amount/frequency/motion field, and `Console::cursor_effects` is a field nothing in Theme resolution reads or writes. A behavioural test would be tautological today — nothing yet resolves `dark_theme`/`light_theme` into `self.theme` — and belongs with `06`/`07`'s pickers.
- Added `console::kittest_tests::no_menu_offers_a_reset_to_theme_defaults_button` (loops the surviving menus) and `console::kittest_tests::glitch_controls_are_offered_only_by_the_settings_menu` (absent before any menu opens, present once Settings does), renamed from `the_theme_menu_offers_no_reset_button`/`glitch_controls_live_in_the_settings_menu_not_the_theme_menu`, which depended on opening a `Theme` menu that no longer exists. Also renamed `a_focused_theme_menu_value_box_…` to `a_focused_settings_menu_value_box_…`, and added a negative assertion to `console::tests::the_bottom_panel_shows_tick_zero_and_run_clock_before_the_first_run` that "Theme" is not on the top bar. Confirmed red by temporarily reinstating `ui.menu_button("Theme", |_ui| {})` and rerunning that test before restoring the fix.
- Named the top menu bar's item gap `MENU_BAR_GAP` instead of repeating the `16.0` literal, reusing it at every call site including the one in the isolated top-panel-height test.
- Trimmed the ADR 0053 rationale that had been restated in most of the touched comments down to one short reference, at the one place it is a placement decision (`console.rs`'s `Settings` menu comment); the rest just describe what the item is now. History (why `06`/`09` moved what they moved) lives in this file's Comments, not in code.
- Defaults, zero-amount/zero-frequency semantics, and the persistence round trip (absent and malformed) were already covered by `cursor_effects.rs`'s and `persistence.rs`'s existing tests from `06`'s work; no behaviour there needed to change for this slice.
- The five remaining unticked lines above are the zero-settings clarification's own scope (opened the same day, above) — repaint scheduling at amount zero, frequency zero's retained fragmentation, reduced motion's effective-vs-stored split, Playback/Run Clock independence, and the focused tests proving all four — and are this issue's next slice of work.

`cargo fmt --all -- --check`, `cargo clippy --package console --all-targets --locked -- -D warnings`, `cargo nextest run --package console --locked` and `cargo nextest run --workspace --tests --no-default-features --locked` all pass at this point.

**2026-09-22 — zero-settings clarification implemented, after rebasing onto `theme-paint-source`.** The base branch moved to a fully named-property Theme format (no more base16 slots) and rescoped this issue's acceptance list to the zero-settings clarification above; rebased this branch onto it, resolving conflicts by taking the base's rewritten `theme.md`/schema content and re-applying only the still-true parts of this issue's own comments and ticks (see the previous entry). This pass then closed the five lines the clarification opened, one seam at a time:

- **`CursorEffectAnimation::repaint_after`** (`cursor_effects.rs`) now requires `amount() > 0 && frequency() > 0` to schedule a wake, not `frequency() > 0` alone. Red: `zero_amount_with_positive_frequency_requests_no_repaint` failed against the old `frequency()`-only condition (`Some(0ns)` instead of `None`). Green: the one-line condition change. This is the only production-code change; `advance` already only changes the sample when `frequency() > 0`, and `cursor_effect_shapes`/`frame_shapes`/`area_shapes` never read frequency at all, so amount zero's "stationary frame without decorative noise" was already painted correctly — only the *scheduling* had the bug the clarification names.
- **Frequency zero retaining fragmentation** was already true (paint never reads frequency); added `frequency_zero_retains_fragmentation_when_amount_is_nonzero`, which freezes a sample at frequency zero and confirms a nonzero amount still paints a fragmented frame and a nonempty area fill through it, and strengthened `zero_frequency_freezes_the_sample_and_requests_no_repaint` with an explicit `amount() > 0` setup assertion so the scenario cannot silently drift back to amount zero.
- **Reduced motion's effective-vs-stored split** was already structurally guaranteed: `respecting_reduced_motion` takes `self` by value on a `Copy` type and returns a new value, and `Console::ui` binds that to a local (`cursor_effect_settings`) rather than writing back to `self.cursor_effects`. Strengthened `reduced_motion_uses_a_clear_static_cursor` to also assert `repaint_after` schedules nothing for the effective (zeroed) settings, and added `console::kittest_tests::reduced_motion_changes_only_the_effective_settings_not_the_stored_ones`, a genuine (non-tautological) Console-level regression test: it sets `reduced_motion = true` and distinctive stored amount/frequency, runs frames, and asserts the stored field is byte-for-byte unchanged — unlike the tautological Theme/mode test the prior review round deleted, this one exercises a real, already-present coupling (`respecting_reduced_motion` called every frame) rather than a nonexistent one.
- **Playback/Run Clock independence from the cursor effect's own deadline** was already guaranteed by `until_next`'s signature (`Option<Duration>` terms, `flatten()`ed and independently minimised) and covered generically by `readout_deadline::tests`. Added `amount_zero_does_not_suppress_the_run_clocks_repaint_schedule` in `cursor_effects.rs`, which composes the real `CursorEffectAnimation::repaint_after` (amount zero → `None`) with the real `until_next`, proving the two pieces this pass touches and relies on compose correctly end to end, not just each in isolation.
- All four scenarios the "Focused tests cover…" line names now have a dedicated test; none of the four needed a change to `Console::ui`'s own wiring (`show_source_scene`'s `cursor_effect_settings`/`cursor_delay`/`until_next` call sites), only the one-line `repaint_after` fix and the paint-level regression proofs above.

`cargo fmt --all -- --check`, `cargo clippy --package console --all-targets --locked -- -D warnings`, `cargo nextest run --package console --locked` (316 passed) and `cargo nextest run --workspace --tests --no-default-features --locked` (1168 passed) all pass.

**2026-09-22 — review: replaced the pure-function composition test with two Console-level ones, trimmed comments.** The prior entry's Playback/Run Clock test (`cursor_effects.rs::amount_zero_does_not_suppress_the_run_clocks_repaint_schedule`) composed `repaint_after` and `until_next` directly and duplicated `readout_deadline::tests::until_next_answers_the_remainder_to_the_next_whole_second`; it could not have caught a regression in `Console::ui`'s own wiring. Replaced it with `console::tests::a_playing_console_still_repaints_when_the_cursor_effect_has_no_deadline`: a real `Console`, Cursor Effect amount zero, Playback started and polled to `Playing`, asserting a quiet pass still requests a repaint within a second via `app_pass_repaint_delay` (the existing helper `a_playing_console_with_cursor_effect_off_wakes_within_a_second` already uses). Confirmed red by temporarily gating the `until_next` call on `cursor_delay` being `Some` — the exact regression shape named in review — which left the delay at `Duration::MAX`; restored and green.

`console::kittest_tests::reduced_motion_changes_only_the_effective_settings_not_the_stored_ones` only checked the stored half and would have passed even if `respecting_reduced_motion` were never called. Moved it to `console::tests` (it needs `app_pass_repaint_delay`, private to that module) and added the effective-side half: after several quiet passes let the "two repaints to settle" grace period lapse, a further quiet pass with Playback stopped must request no repaint at all (`Duration::MAX`) — the same invariant `a_click_still_pans_to_follow_the_cursor_under_reduced_motion_with_playback_stopped`'s own doc already names ("the one combination in which Console's own repaint scheduling asks for nothing at all"). Confirmed red by replacing `self.cursor_effects.respecting_reduced_motion(self.reduced_motion)` with `self.cursor_effects` (i.e. reduced motion doing nothing): the stored assertions still passed, but the new delay assertion failed with a real scheduled repaint; restored and green.

**`wake_panel_when_playback_publishes` is not covered, and not cleanly coverable.** It calls `ctx.request_repaint()` directly from a background task on every Playback publish, independent of any `Console::ui` pass. Whenever Playback is actually `Playing`, `Console::ui`'s own per-frame `until_next` call also requests a repaint for the same moving Run Clock, on the same shared `Context` — so a pass's recorded `repaint_delay` cannot say which of the two asked. Nor can waiting between passes and checking `ctx.has_requested_repaint()` isolate it: egui's own "two repaints to let things settle" rule (`egui-0.36.2/src/context.rs:128-137`) means an earlier request from either source keeps the flag true regardless of whether the background task fires again. The one distinguishing signal, `Context::repaint_causes()`'s `RepaintCause::line`, would mean asserting against `wake_panel_when_playback_publishes`'s call-site line number — a test that breaks on an unrelated edit above it, not a public interface. Isolating the background task cleanly would need a seam cut into shipped code (an injectable callback or counter) that CLAUDE.md's "keep test-only inputs out of shipped code" rules out. Left uncovered.

Tightened comments to describe current behaviour only: dropped the "used to be enough … now has an equal veto" / "now vetoed by either field" before/after framing from `repaint_after`'s callers, and the fragile by-name cross-reference to `cursor_effects.rs::tests::reduced_motion_uses_a_clear_static_cursor` from the (now-replaced) kittest doc comment. Extracted a `settings(amount, frequency)` test helper in `cursor_effects.rs`, replacing the repeated `let mut settings = CursorEffectSettings::default(); *field_mut() = 0; assert!(other_field() > 0, "test setup…")` shape in four tests with one explicit call each. `frequency_zero_retains_fragmentation_when_amount_is_nonzero` is kept as written — a regression guard for behaviour that was already correct, not a claim that anything changed.

`cargo fmt --all -- --check`, `cargo clippy --package console --all-targets --locked -- -D warnings` and `PROPTEST_CASES=32 cargo nextest run --package console --locked` all pass.
