# Orcvs

## Building

After installing [Rust](https://rustup.rs/), you can compile Orcvs as follows:

```shell
cargo xtask bundle orcvs --release
```


launchctl setenv NIH_LOG /Users/tobyhede/src-p/orcvs/reaper.log