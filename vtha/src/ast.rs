/*

    fn a b c

    fn (a b c) (a b c)

    fn a (fn a fn 1) fn 1

    Function Vec<Atom>

    Function Vec<Vec<Atom>>>

    Expression
    Function(Function, Vec<Atom>)

*/

#[cfg(test)]
mod test {
    use crate::{ArgumentError, VthaError};

    #[test]
    fn test_play_function() {}
}
